
<div class="form-actions">
<%= link_to "Back", homes_path, :class => 'btn'  %>
<%= link_to "Edit", edit_home_path(@home), :class => 'btn' %>
<%= link_to "Delete", home_path(@home), :method => "delete", :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}", :class => 'btn' %>
</div>
